# Git Automation Rules

- Automatically stage, commit, and push changes (`git add .`, `git commit -m "...", git push origin main`) whenever code modifications, bug fixes, or visual adjustments are made.
- Do not ask for user confirmation or permission to run `git add` or commit/push commands.
- Run builds (`npm run build`) and git sync commands automatically upon completing code updates.
