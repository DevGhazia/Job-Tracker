// Static metadata generation is deprecated.
// OAuth discovery is now served dynamically by serverless functions in /api.
console.log("OAuth metadata is served dynamically by serverless API handlers.");
